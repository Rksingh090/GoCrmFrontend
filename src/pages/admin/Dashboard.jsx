import { Hammer } from 'lucide-react'
import React from 'react'
import { useAppCtx } from '../../context/appContext'

const Dashboard = () => {
  const {themeState} = useAppCtx()
  return (
    <div>
      
    </div>
  )
}

export default Dashboard